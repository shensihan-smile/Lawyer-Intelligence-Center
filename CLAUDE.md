# 律师智能中心 (Lawyer Intelligent Center)

## 项目概述

本项目是一款面向律师的 Windows 桌面应用程序，旨在通过智能化工具提升律师工作效率。核心价值：将分散的沟通消息、案件文档、庭审日程、财务数据集中管理，并通过 AI 技术实现自动化和智能化处理。

### 目标用户
- 执业律师（个体或小型律所）
- 法务工作者
- 律所运营管理人员

### 项目核心规则：技术决策必须选择题化 ⚠️

**关键规则**：本项目中的所有技术决策（包括但不限于框架选择、库的引入、架构方案、UI 设计、数据库选型等），开发人员（Claude Code）必须：
1. 提供 2-4 个可行方案
2. 用通俗语言（非技术人士能理解）解释每个方案
3. 列出每个方案的优点和缺点
4. 给出推荐建议
5. 由用户（律师，非技术人员）做出最终选择

**绝对禁止**：自行决定技术方案而不给用户选择权。

---

## 技术栈

| 层面 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 桌面壳 | Electron | 33+ | 将 Web 技术封装为 Windows 桌面应用 |
| 前端框架 | React + TypeScript | 19 | 构建用户界面 |
| UI 组件库 | Ant Design | 5 | 企业级 UI 组件，表格/表单/日历丰富 |
| 后端服务 | Python FastAPI | 最新稳定版 | 高性能 API 框架 |
| 数据库 | SQLite | - | 单文件本地数据库，无需安装服务器 |
| 文档处理 | python-docx, PyMuPDF, Pillow | - | Word/PDF/图片处理 |
| AI 接口 | OpenAI SDK | - | 对接大模型 |
| 构建打包 | electron-builder | - | 一键打包为 Windows 安装程序 |

---

## 功能需求

### 模块一：智能通信管理模块

#### 1.1 消息识别
**功能 1.1.1：微信/企微群消息识别**
- **描述**：从微信群或企业微信群中捕获客户消息，AI 自动识别意图并生成待办任务
- **阶段 1 实现**：手动输入替代自动捕获（用户在界面中粘贴或录入消息内容）
- **阶段 2 实现**：接入 AI 大模型，自动解析消息提取关键信息（客户名、案由、需求等），生成待办事项
- **验收标准**：
  - 手动录入消息后，AI 能正确识别客户意图
  - 自动生成包含优先级、截止时间的待办任务
  - 待办任务可关联到现有案件或客户

#### 1.2 客户管理
**功能 1.2.1：客户档案维护（客户信息库）**
- **描述**：建立统一的客户基础档案，沉淀联系方式、合作历史、法务对接人及关联案件
- **信息字段**：
  - 基本信息：客户名称、联系人、电话、微信、邮箱、地址
  - 合作历史：过往委托记录、案件列表
  - 法务对接人：对方当事人、对方律师、法官等
  - 关联案件：当前进行的案件列表及状态
- **验收标准**：
  - 支持客户档案的增删改查
  - 支持从案件页面快速跳转到客户页面
  - 支持按名称、联系方式等信息搜索和筛选

---

### 模块二：案件与文档中心模块

#### 2.1 案件管理
**功能 2.1.1：案件立项与维护（案件信息主页）**
- **描述**：记录案号、案由、当事人、标的额、当前阶段等核心信息，作为系统数据的关联主干
- **信息字段**：
  - 基本信息：案号、案由、受理法院、承办法官、书记员
  - 当事人信息：原告/被告/第三人及其代理人信息
  - 标的额：诉讼标的金额
  - 案件阶段：接案 → 立案 → 审理中 → 判决 → 执行 → 结案
  - 时间轴：收案日期、立案日期、开庭日期、判决日期、结案日期
- **验收标准**：
  - 支持案件全生命周期状态流转
  - 支持按阶段、法院、时间等维度筛选和搜索

**功能 2.1.2：履约文件记录（文件归档与绑定）**
- **描述**：记录并上传向客户出具的文件（如法律意见书、合同草案），自动关联至对应案件与客户
- **支持文件类型**：法律意见书、合同草稿、起诉状、答辩状、代理词、证据清单等
- **文档元数据**：文件类型、上传时间、版本号、起草人
- **验收标准**：
  - 支持拖拽或点击上传文件
  - 上传后自动归类到对应案件和客户
  - 支持按案件、客户、文档类型筛选

---

### 模块三：智能文档处理模块

#### 3.1 文档格式转换
**功能 3.1.1：PDF 与 Word 互转及编辑**
- **描述**：支持将 Word 文档编辑及一键转换为 PDF 以固化排版，或将 PDF 解析为可编辑的 Word 文档
- **转换能力**：
  - PDF → Word：保持原排版格式，支持中文
  - Word → PDF：一键固化排版，适合发送法院或客户
  - 基本编辑：文字调整、格式修改
- **验收标准**：
  - 转换后保留原始文档排版格式
  - 支持中文文档的完整转换（字体、表格、图片不丢失）

#### 3.2 文档图片化
**功能 3.2.1：生成长图/分页图（图像导出）**
- **描述**：将 Word 或 PDF 文档整体导出为高清长图，或按页生成多张图片，便于移动端预览与微信分享
- **导出选项**：
  - 连续长图（适合手机阅读和分享）
  - 分页图片（每页一张图，适合逐页查看）
  - 可选水印：律所名称、律师姓名
- **验收标准**：
  - 生成图片清晰可读（分辨率 ≥ 150 DPI）
  - 长图模式下各页之间合理分隔
  - 支持批量导出多份文档

---

### 模块四：日程与庭审中心模块

#### 4.1 短信解析
**功能 4.1.1：12368 短信识别（庭审信息提取）**
- **描述**：自动读取并解析 12368 法院服务平台的庭审通知短信，精准提取开庭时间、地点、案号、法官及审判庭信息
- **提取字段**：
  - 开庭时间（日期 + 具体时间）
  - 法庭地点（法院名称 + 具体法庭）
  - 案号
  - 承办法官姓名
  - 联系方式
- **实现方式**：
  - 阶段 1：手动粘贴短信原文，AI 解析提取
  - 阶段 2：自动监听手机短信（需配合手机端方案）
- **验收标准**：
  - 正确解析标准格式的 12368 短信
  - 自动匹配到已有案件
  - 容错处理：短信格式不完全标准时也能尽量提取关键信息

#### 4.2 日程管理
**功能 4.2.1：庭审日历调度（自动排期与冲突预警）**
- **描述**：基于解析结果自动生成庭审日历，若同一时间段存在多个日程则触发冲突预警
- **功能要点**：
  - 日历视图：日视图 / 周视图 / 月视图
  - 自动排期：从短信解析结果自动生成日程
  - 冲突检测：同一时间段两个开庭 → 标红预警
  - 手动添加：支持手动录入会议、咨询等非开庭事务
- **验收标准**：
  - 冲突时自动标红并弹出提醒
  - 支持导出日历（iCal 格式，可导入手机日历）

#### 4.3 消息提醒
**功能 4.3.1：自动化通知推送（多节点提醒）**
- **描述**：在开庭前（如 3 天/1 天/2 小时）自动通过系统弹窗进行通知提醒
- **提醒规则**：
  - 默认节点：开庭前 3 天、1 天、2 小时
  - 提醒方式：Windows 系统通知弹窗
  - 支持自定义提醒时间和方式
- **验收标准**：
  - 系统托盘图标常驻，有新提醒时闪烁
  - 弹窗通知显示案件基本信息（案号、时间、地点）

---

### 模块五：财务账单管理模块

#### 5.1 计费引擎
**功能 5.1.1：服务数据聚合（计费明细生成）**
- **描述**：自动抓取"出具文件记录"与"处理案件动作"，按照设定的收费标准转化为计费条目
- **计费标准配置**：
  - 按小时计费（设置小时费率）
  - 按件计费（设置单件固定费用）
  - 按标的额比例计费（设置比例和阶梯）
  - 混合模式（不同案件不同标准）
- **验收标准**：
  - 配置计费标准后自动计算费用
  - 支持手动调整和补充计费项
  - 导出计费明细表格

#### 5.2 账单生成
**功能 5.2.1：工时记录（智能工时统计）**
- **描述**：记录系统内各类事项的处理时长，作为服务计费与账单明细的数据支撑
- **记录方式**：
  - 计时器：开始/暂停/停止计时
  - 手动补录：事后填写工作时长
- **工作分类**：法律研究、文书起草、庭审出庭、客户咨询、其他
- **验收标准**：
  - 计时器准确记录到分钟
  - 支持事后手动补录和修改

**功能 5.2.2：周期账单输出（一键出账与导出）**
- **描述**：设定月结或年结规则，月底/年底自动生成包含服务明细的标准化对账单，支持 PDF 导出
- **账单内容**：
  - 律所基本信息
  - 客户信息
  - 计费期间
  - 服务明细列表
  - 费用汇总
  - 付款信息
- **验收标准**：
  - 账单格式规范，适合直接发给客户
  - 支持按月/季/年生成账单
  - PDF 格式导出
  - 支持历史账单查询

---

### 模块六：系统基础管理模块

#### 6.1 权限控制
**功能 6.1.1：账号与组织架构（人员及岗位分配）**
- **描述**：建立内部团队账号体系，根据职级分配不同的数据查阅与操作修改权限
- **组织架构**：律所 → 部门/团队 → 个人
- **预设角色**：
  - 管理员：全部功能和数据访问权限
  - 合伙人：可查看全所数据，管理团队
  - 执业律师：管理自己的案件和客户
  - 律师助理：协助律师处理事务，权限受限
  - 实习生：仅查看被分配的案件
- **验收标准**：
  - 支持创建/编辑/停用用户账号
  - 不同角色登录后看到的菜单和数据范围不同
  - 权限控制到功能按钮级别

---

## 项目约定

### 命名规范
- 前端代码文件：使用英文驼峰命名（如 `CaseManagement.tsx`、`ClientProfile.tsx`）
- 后端 API 路由：RESTful 风格，URL 使用英文复数名词（如 `/api/cases`、`/api/clients`）
- 数据库表名：英文小写加下划线（如 `case_info`、`client_info`）
- 数据库字段名：英文小写加下划线（如 `case_number`、`client_name`）
- Git 提交信息：使用中文简要描述（如"完成案件管理模块基本功能"）

### 文件组织
- 前端页面按模块划分目录，每个模块独立文件夹
- 后端 API 按功能模块划分，每个模块一个路由文件
- 通用组件放在 `frontend/src/renderer/components/` 目录下
- 所有配置文件放在项目根目录

### 开发流程
1. 用户提出功能需求
2. Claude Code 提出 2-4 个技术方案并解释
3. 用户选择方案
4. Claude Code 实现
5. 用户验收测试
6. 如有问题，Claude Code 修复

### 数据管理
- 使用 SQLite 本地数据库（单文件，无需安装数据库服务器）
- 数据库文件位于 `backend/data/` 目录
- 定期备份数据库文件（建议每周自动备份）
- 开发阶段数据库变更使用 Alembic 进行迁移管理

### 隐私与安全
- 所有数据存储在本地，不上传云端（除非用户明确配置云端同步）
- AI 功能调用需用户明确授权，且只发送用户明确选择的内容
- 敏感数据（当事人信息、案件材料）在数据库中加密存储
- 用户密码使用 bcrypt 哈希加密

---

## 实施路线图

### 第一阶段：基础设施 + 系统管理 ✔（当前阶段）
- 搭建 Electron + React 前端框架
- 搭建 Python FastAPI 后端框架
- SQLite 数据库设计与建表
- 实现用户登录、组织架构、角色权限（模块六）
- 搭建基础 UI 框架（左侧导航、顶部栏、内容区域布局）

### 第二阶段：核心业务 — 案件与客户管理
- 实现案件登记与维护（2.1.1）
- 实现客户档案管理（1.2.1）
- 实现文档上传与归档（2.1.2）
- 实现案件-客户-文档的关联关系

### 第三阶段：文档处理能力
- 实现 PDF ↔ Word 互转（3.1.1）
- 实现文档生成长图和分页图（3.2.1）
- 实现文件在线预览

### 第四阶段：日程与庭审管理
- 实现 12368 短信解析（4.1.1）
- 实现日历排期与冲突检测（4.2.1）
- 实现系统提醒通知（4.3.1）

### 第五阶段：财务管理
- 实现计费引擎（5.1.1）
- 实现工时记录（5.2.1）
- 实现定期账单生成与 PDF 导出（5.2.2）

### 第六阶段：AI 增强 + 集成测试
- 实现消息手动录入和 AI 识别（1.1.1）
- 集成 AI 大模型能力到各模块
- 系统集成测试和性能优化
- 打包为 Windows 安装程序

---

## 常见问题（FAQ）

*（开发过程中逐步填充）*

---

> 📌 **给 AI 助手的提醒**：本文件是所有讨论的起点。每次开发新功能前，请先阅读本文件中的相关功能描述和验收标准。遇到技术决策时，务必遵循"技术决策选择题化"规则。
